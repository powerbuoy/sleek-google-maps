Adds a "Google Maps API Key" setting to the admin which, if not empty, adds the Google Maps API to the page with a `gmAsyncInit` callback. Also sends the Google Maps Key to ACF so that ACF maps work.
